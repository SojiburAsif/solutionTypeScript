

No.1 TypeScript এ Interface এবং Type এর মধ্যে পার্থক্য (What are some differences between interfaces and types in TypeScript?)
Answer:TypeScript এ আমরা interface এবং type দুটোই ব্যবহার করি নতুন টাইপ ডিফাইন করার জন্য। তবে এদের মধ্যে কিছু পার্থক্য আছে।

Interface:
সাধারণত অবজেক্ট বা ক্লাসের স্ট্রাকচার ডিফাইন করতে ব্যবহার করা হয়।
একাধিক interface একত্রিত করা যায় (Declaration Merging)।
ইন্টারফেস বর্ধিত (extend) করা যায়।

উদাহরণ:

interface Person {
  name: string;
  age: number;
}
interface Employee extends Person {
  salary: number;
}

const emp: Employee = { name: "Asif", age: 19, salary: 50000 };


Type:
প্রিমিটিভ, ইউনিয়ন, টুপল সহ যেকোনো টাইপ ডিফাইন করতে পারি।
Type alias merge করা যায় না।
একাধিক টাইপকে একত্রিত করতে & (Intersection) ব্যবহার করতে হয়।

উদাহরণ:

type ID = string | number;
type EmployeeType = Person & { salary: number };
const emp2: EmployeeType = { name: "Rakib", age: 25, salary: 40000 };

সারসংক্ষেপ:
যদি অবজেক্ট বা ক্লাসের স্ট্রাকচার কেবল প্রকাশ করতে হয় → Interface ব্যবহার।
যদি জটিল টাইপ, ইউনিয়ন বা টুপল ব্যবহার করতে হয় → Type alias ব্যবহার।







----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------


No.2
Union Type (|) কী?
Answer: Union Type হলো TypeScript এর একটি ফিচার যা বলে, একটি ভেরিয়েবল একাধিক টাইপের মধ্যে যেকোনো একটিতে থাকতে পারে। অর্থাৎ, সেই ভেরিয়েবল নির্দিষ্ট কিছু টাইপের মান গ্রহণ করতে পারে।

উদাহরণ:
type UserRole = 'admin' | 'user' | 'guest';
type Status = 'active' | 'inactive' | 'pending';

let myRole: UserRole;
myRole = 'admin';  ঠিক আছে
myRole = 'user';    ঠিক আছে
myRole = 'superuser';   error, কারণ superuser এই Union-এ নেই

let accountStatus: Status;
accountStatus = 'active';    ঠিক আছে
accountStatus = 'pending';   ঠিক আছে
accountStatus = 'blocked';  error


Intersection Type (&) কী? 

Answer: Intersection Type হলো TypeScript এর একটি ফিচার যা দুই বা ততোধিক টাইপকে একত্রিত করে। অর্থাৎ, নতুন টাইপে সব টাইপের প্রপার্টি একসাথে থাকবে।

উদাহরণ:

type Employee = { 
  id: number; 
  name: string; 
  phoneNo: number; 
};

type Manager = { 
  designation: string; 
  teamSize: number; 
};

type EmployeeManager = Employee & Manager;
const allEmployee: EmployeeManager = {
  id: 1,
  name: 'Asif',
  phoneNo: 1234567890,
  designation: 'Programming Hero',
  teamSize: 10
};

console.log(allEmployee);

ব্যাখ্যা:

এখানে EmployeeManager হলো Employee আর Manager দুইটির intersection।
অর্থাৎ EmployeeManager-এ Employee এর সব প্রপার্টি এবং Manager এর সব প্রপার্টি থাকতে হবে।
কোন প্রপার্টি বাদ দিলে TypeScript error দেবে।
সংক্ষেপে:

& ব্যবহার করে একাধিক টাইপের সব প্রপার্টি একসাথে মিশানো যায়।
Union Type (|) এর মতো নয়, যেখানে একাধিক টাইপের মধ্যে যেকোনো একটি নেওয়া যায়।
Intersection Type সব টাইপের বৈশিষ্ট্যই বাধ্যতামূলক করে দেয়।

💡 মনে রাখার কথা:
| = Either type (কোনো একটাই মান নেওয়া যাবে)
& = Combine type (সব টাইপের মান থাকতে হবে)
